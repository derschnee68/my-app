import React, { Component } from 'react';

class ButtonComponent extends Component {
  render() {
    return <button>BGGG button</button>;
  }
}

export default ButtonComponent; // Don’t forget to use export default!